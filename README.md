# bun-start

Minimal Bun TypeScript playground.

## Requirements

[Docker](https://www.docker.com)

## Usage

```bash
# run both services in parallel
docker compose up

# run a single service
docker compose up bun
docker compose up test
```
